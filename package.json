{
  "name": "flocky",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "streams": "node tools/set-stream.mjs"
  },
  "engines": { "node": ">=22" }
}
